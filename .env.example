# Copy this file to .env for local development and fill in real values.
# On Railway, set these as service Variables instead (never commit .env).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Facilitator-only actions (uploading/changing the passage, asking the 3 LLMs)
# are gated behind this shared code. Pick something easy to say aloud in class.
FACILITATOR_ACCESS_CODE=
